{
  "name": "maxi-sommeil",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "expo start",
    "prebuild:android": "expo prebuild --platform android --clean",
    "build:android:apk": "npm run prebuild:android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "expo": "~57.0.14",
    "react": "19.2.3",
    "react-native": "0.86.0"
  },
  "devDependencies": {
    "@babel/core": "^7.25.2"
  }
}
