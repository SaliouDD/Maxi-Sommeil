{
  "name": "maxi-sommeil",
  "version": "1.0.0",
  "private": true,
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "web": "expo start --web",
    "prebuild:android": "expo prebuild --platform android --clean",
    "build:android:apk": "expo prebuild --platform android --clean && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "expo": "~51.0.39",
    "expo-status-bar": "~1.12.1",
    "react": "18.2.0",
    "react-native": "0.74.5"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0"
  }
}
